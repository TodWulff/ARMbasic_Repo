#include "LIB\LCP2103.lib"

#define SPI_OK		0
#define SPI_BUSY	1
#define SPI_ERROR	2

DIM SPI_STATE AS INTEGER
DIM SPI_BUF(4) AS INTEGER
DIM SPI_MSG_PTR AS INTEGER
DIM SPI_COUNT AS INTEGER


SUB	SPI_ISR

	IF S0SPCR and $F8 = $80 then

	ELSE
	
	ENDIF
ENDSUB